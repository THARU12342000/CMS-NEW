import React from 'react';

const Dashboard = () => {
  return (
    <div>
      <h2>Admin Dashboard</h2>
      <p>Welcome, Admin. Use the side menu to manage users, products, orders, and consents.</p>
    </div>
  );
};

export default Dashboard;
